export enum EntityType {
    CONFIG = 'config',
    CUSTOMERS = 'customers',
    PRODUCTS = 'products',
    ORDERS = 'orders',
    ITEMS = 'items'
}

export enum ActionType {
    GET = 'GET',
    FIND = 'FIND',
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
    DELETE = 'DELETE'
}

export enum SeverityType {
    INFO = 'info',
    WARNING = 'warning',
    ERROR = 'error',
    SUCCESS = 'success',
    FAILED = 'failed'
}
