export enum EntityType {
    CUSTOMERS = 'customers',
    PRODUCTS = 'products',
    ORDERS = 'orders',
    ITEMS = 'items'
}

export enum ActionType {
    GET = 'GET',
    FIND = 'FIND',
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
    DELETE = 'DELETE'
}