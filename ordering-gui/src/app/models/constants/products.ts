export enum Currency {
    USD = '$'
}

export enum GetProductActionType {
    LOADING = '[products] GET LOADING',
    SUCCESS = '[products] GET SUCCESS',
    ERROR = '[products] GET ERROR'
}

export enum FindProductsActionType {
    LOADING = '[products] FIND LOADING',
    SUCCESS = '[products] FIND SUCCESS',
    ERROR = '[products] FIND ERROR'
}
