version: '3.5'

services:
  orders:
    image: openjdk:8-jre-alpine
      container_name: orders
      ports:
      - 9000:9000
      environment:
      - SPRING_PROFILES_ACTIVE=default
      networks:
      - kafka

networks:
  kafka:
    external: true
