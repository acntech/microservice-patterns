version: '3.5'

services:
  customers-service:
    image: openjdk:8-jre-alpine
      container_name: oauth2-token-relay-customers-service
      ports:
        - 9000:9000
      environment:
        - SPRING_PROFILES_ACTIVE=default
