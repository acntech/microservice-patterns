########################
# SPRING Configuration #
########################
spring:
  profiles:
    include:
      - postgres
      - development
      - test-data
  datasource:
    url: jdbc:postgresql://localhost:5432/customers

# Logging
logging:
  level:
    org.springframework: INFO
    org.hibernate: WARN
    no.acntech: DEBUG
