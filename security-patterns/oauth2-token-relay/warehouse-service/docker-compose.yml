version: '3.5'

services:
  warehouse-service:
    image: openjdk:8-jre-alpine
      container_name: oauth2-token-relay-warehouse-service
      ports:
        - 9040:9040
      environment:
        - SPRING_PROFILES_ACTIVE=default
