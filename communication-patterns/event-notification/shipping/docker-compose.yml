version: '3.5'

services:
  shipping:
    image: openjdk:8-jre-alpine
      container_name: event-notification-shipping
      ports:
        - 9030:9030
      environment:
        - SPRING_PROFILES_ACTIVE=default
      networks:
        - kafka

networks:
  kafka:
    external: true
